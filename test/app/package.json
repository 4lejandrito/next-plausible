{
  "name": "next-plausible-test-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "next build && next export && jest",
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  }
}
